{
  "name": "create-fullstacked",
  "version": "0.1.4-rc2",
  "description": "Create a FullStacked Project",
  "bin": {
    "create-fullstacked": "index.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/CPLepage/create-fullstacked.git"
  },
  "scripts": {
    "build": "npx esbuild index.ts --outfile=index.js --format=cjs",
    "pretest": "npm run build && npx esbuild test.ts --outfile=test.js --format=cjs",
    "test": "node test",
    "prepublishOnly": "npm run build"
  },
  "author": "CP Lepage",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/CPLepage/create-fullstacked/issues"
  },
  "homepage": "https://fullstacked.org",
  "devDependencies": {
    "fullstacked": "^0.9.0-rc3"
  },
  "main": "index.js"
}
